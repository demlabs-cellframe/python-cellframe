# Test dependencies for python-cellframe
# Install with: pip install -r requirements-test.txt

# Core dependencies (needed for tests to run)
pydantic>=2.0.0
typing-extensions>=4.0.0

# Testing frameworks
pytest>=6.0
pytest-cov
pytest-mock
pytest-asyncio
pytest-benchmark
pytest-xdist
hypothesis

# Code quality tools
coverage
mypy
black
isort
flake8

# Performance profiling
memory-profiler
psutil

# Build tools (for wheel creation)
build
wheel
setuptools
twine
